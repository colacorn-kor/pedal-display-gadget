# CONTROLLER_DESIGN.md — 외부 6키 컨트롤러 설계 (2단 전략)

> **위상:** 외부 컨트롤러의 *물리 인터페이스 계약*과 *2단 로드맵*에 대한 SSOT.
> 넷 연결의 구체값은 `hardware/NETLIST_SPEC.md §7`, 조립 절차는 `ASSEMBLY.md`가 담당한다.
> 이 문서는 "왜 그 값인가"와 "나중에 무엇이 바뀌고 무엇이 안 바뀌는가"를 고정한다.

---

## 1. 왜 2단인가

6키 컨트롤러는 **가젯의 기본 내비게이터**다. MIDI로 대체하지 않는다. 이유:

- 키캡 커스텀·페달보드 내 자유 배치가 가능해야 한다
- 기타 유저에게 익숙한 TRS/TS 잭으로 충분하다
- **설정할 때만 꽂고, 실연주 중엔 뽑은 채 풋스위치만 쓰는** 사용 패턴을 지원한다
- 제품을 처음 만난 사람이 **즉시 plug-and-play** 되어야 한다 (MIDI는 초보 진입장벽이 높다)

MIDI는 이와 별개로 **프로 사용자용 확장 출입구**로 Phase 2에 유지한다(→ `PROJECT_MASTER.md S4`).

그래서 컨트롤러를 두 등급으로 나눈다. 마샬이 단순 2버튼 스위치와 스마트 6버튼 컨트롤러를
같은 잭에서 모두 받는 것과 같은 구조다.

| | **Basic (저항 래더)** | **Smart (MCU)** |
|---|---|---|
| 구현 | 저항 6개, 능동소자 0 | MCU + 오픈드레인 통신 |
| 입력 | 단일 키만 | 다중키·모디파이어 |
| 지연 | 약 30ms | 5ms 이하 가능 |
| LED | 없음 | 테마 연동 앰비언트 조명 |
| 핫플러그 | 삽입 중 오입력 가능 | 프레이밍/체크섬으로 차단 |
| 원가 | 수백 원 | 수천 원 |
| 상태 | **현재 구현됨** | Phase 2 |

Basic은 버려지는 프로토타입이 아니라 **최종 제품의 보급형 라인**이다.

---

## 2. 물리 인터페이스 계약 (고정 — 변경 금지)

두 등급이 **같은 잭·같은 케이블**을 공유하려면 아래가 고정되어야 한다.
PCB 설계 시 이 계약을 반드시 따른다.

```
TRS 잭 (본체측)
  Sleeve : GND
  Ring   : +3V3  ← 고정
  Tip    : 신호 (Basic=ADC 입력 / Smart=오픈드레인 양방향 데이터)
```

### 2-1. ⚠ Ring은 +3V3으로 고정한다. 5V 금지.

**이 계약을 어기면 되돌릴 수 없는 사고가 난다.** Ring을 5V로 올린 상태에서 Basic 컨트롤러를
꽂으면 무입력 전압이 그대로 5V가 되어 Tip을 통해 G4로 들어가고, **ESP32-S3 ADC 절대 최대
정격을 초과해 핀이 손상된다.**

LED를 더 밝게 쓰고 싶어도 Ring 전압을 올리지 않는다. 필요하면 **Smart 컨트롤러 내부에서
국소 부스트**한다. 어차피 DevKit의 3V3 레일 여유가 총량을 결정하므로 전압을 올려도
사용 가능 전력은 늘지 않는다.

### 2-2. Rtop 10k — 두 등급이 공유하는 부품

Basic에서는 분압기의 상단 저항, Smart에서는 **오픈드레인 버스의 풀업**으로 쓰인다.
역할만 바뀌고 부품과 위치는 동일하다. 등급 전환 시 **떼어낼 필요가 없다.**

### 2-3. Tip 직렬 220Ω (보호)

ADC는 하이임피던스라 전압 분배에 영향이 없다(오차 0.002% 미만). 대신 Smart 모드에서
오픈드레인 충돌·오배선 시 고장 전류를 제한한다. **Basic 단계에서 미리 넣어둔다.**

### 2-4. Ring 직렬 100Ω 또는 폴리스위치 (TS 케이블 대비)

TRS 잭에 **TS 플러그를 꽂으면 Ring이 Sleeve에 단락**된다. Ring이 +3V3이므로 이는
3V3 레일 직접 단락이며, 보호가 없으면 가젯 전체가 브라운아웃된다.

기타 유저의 페달보드에는 TS 패치케이블이 널려 있어 **오삽입은 반드시 일어난다.**
따라서 Ring 급전 경로에 보호를 넣는다:

- **Basic 시대:** 직렬 100Ω. 단락 전류를 33mA로 제한. 래더 소비가 0.33mA뿐이라
  전압 강하 33mV로 무시 가능하며, 캘리브레이션이 흡수한다.
- **Smart 시대:** 100Ω으로는 LED 전류를 못 버틴다. **검출 후 급전** 방식으로 교체 —
  약한 풀업으로 Ring 상태를 먼저 확인하고, 정상 TRS일 때만 하이사이드 스위치로 전원을 켠다.

### 2-5. G4는 겸용 포트로 문서화한다

PCB에서 G4를 ADC 전용으로 취급해 과도한 RC 필터를 박으면 Smart 모드의 통신이 불가능해진다.
**필터는 100nF 이하로 제한**하고, 넷 이름도 `TRS_SIG`처럼 용도 중립적으로 둔다.

---

## 3. 등급 자동 판별 (Smart 도입 시)

부팅 시 Tip을 잠시 디지털 입력으로 두고 약 500ms 대기한다.

- **hello 패킷 수신** → Smart 모드. Tip을 UART/오픈드레인으로 운용.
- **무응답** → Basic 모드. Tip을 ADC로 전환해 래더 판정.

Basic 컨트롤러는 눌러도 **정적 DC 레벨**만 만들기 때문에 UART 프레이밍으로 오인될 수 없다.
판별이 깨끗하게 성립한다. 런타임 재판별(핫플러그 대응)은 Smart 구현 시 함께 설계한다.

---

## 4. 등급 전환 시 이관/폐기 목록

Smart로 옮길 때 실제로 버려지는 것을 명확히 해둔다.

**그대로 재사용 (이관 비용 0)**
- TRS 잭·케이블·풋스위치·키캡·인클로저
- Tip→G4 배선, Rtop 10k, Tip 직렬 220Ω, Sleeve/GND
- 입력 이벤트 레이어 전체 — `input_button_t`, 디바운스·홀드·오토리피트, `EV_*` 9종
- 런처·테마·앱·렌더러 전 계층

**폐기**
- 래더 저항 5개 (수백 원)
- `input_ladder_*` 디코드 코드 약 300줄
- 래더 판정표 상수

전환 지점은 **`input_button_read_raw()` 한 함수**다. 이 함수가 raw level의 출처를 캡슐화하고
있으므로, ADC 디코드를 시리얼 수신으로 교체하면 상위 계층은 전혀 모른 채 동작한다.
이 추상화는 우연이 아니라 이 전환을 위해 설계된 것이다.

---

## 5. Smart 컨트롤러 설계 메모 (Phase 2 착수 시)

- **통신:** 오픈드레인 반이중. 양쪽 모두 Tip을 GND로 끌어내리고, Rtop 10k가 풀업.
  프레이밍 + 체크섬 필수(핫플러그 중 쓰레기 신호 차단이 Smart의 핵심 이점).
- **전원 예산:** 3V3 레일 여유 기준 100~200mA. RGB LED 6개 상시 점등은 이 안에서 가능.
  다만 비주얼라이저 연동으로 밝게 쓰려면 컨트롤러 내부 부스트 또는 듀티 제한을 고려.
- **LED 목적:** 키별 상태 표시가 주목적이 **아니다.** 테마 연동 앰비언트 조명 —
  가젯 테마가 바뀌면 컨트롤러 색도 따라 바뀌는, **보조 디스플레이 경로**로 본다.
  인터랙티브 사운드 비주얼라이저라는 제품 정체성의 연장선.
- **TS 케이블 지원:** TS는 Ring이 없어 전원 공급 불가 → LED 없이 통신만 가능한
  축소 모드. 팬텀 급전(Tip 단선 전원+데이터)까지 갈지는 Phase 2에서 결정.
- **MCU 후보:** CH32V003 / ATtiny 계열. 6키 스캔 + 6 LED + 1-wire면 충분.

---

## 6. 현재 결정 사항 요약

| 항목 | 결정 | 상태 |
|---|---|---|
| 6키를 MIDI로 대체 | **하지 않음** | 확정 |
| 4선 케이블(I2C) | **채택하지 않음** (페달보드에 부자연스러움) | 확정 |
| Ring 전압 | **+3V3 고정, 5V 금지** | 확정 |
| Rtop | 10k (Basic 분압 / Smart 풀업 겸용) | 확정 |
| Tip 직렬 220Ω | 넣는다 | 확정 |
| Ring 보호 | Basic=직렬 100Ω / Smart=검출 후 급전 | 확정 |
| Basic→Smart 전환 시점 | **실사용 체감 후 판단** | 보류 |

마지막 항목이 핵심이다. 현재 반응성 우려는 예측이지 체감이 아니다.
Basic을 실제로 며칠 써본 뒤, 답답함이나 다중키 부재가 실제 불편으로 확인되면
그것이 Smart 착수의 근거가 된다. 그때까지 Basic으로 앱을 다듬는다.
