# GG 제품 정의

> 이 문서는 **GG의 제품 정체성·범위·외부 확장 계약 SSOT**다. 구현 세부는
> `ARCHITECTURE.md`, 회로는 `hardware/NETLIST_SPEC.md`, 현재 실기 상태는
> `LAB_STATE.md`가 각각 우선한다.

## 1. 제품 한 문장

**GG는 기타 신호를 손상시키지 않고 통과시키면서, 그 신호의 음정·주파수 분포·절대 레벨을
읽어 화면과 외부 미터로 보여 주는 모듈형 사운드 디스플레이다.**

게임과 이미지 기능은 확장성을 보여 주는 부가 기능이며, 오디오 분석과 무손실 Thru가 제품의
우선순위다.

## 2. 제품 구성

### 본체

- ESP32-S3, 480x320 디스플레이, 풋스위치
- 6.35mm 입력과 하드와이어 Thru 출력
- 자동 듀얼레인지 오디오 분석 탭과 PCM1808 ADC
- MIDI 입출력 단자
- 9V DC 입력
- 개발·업데이트용 USB와 외부 장치용 USB Host
- AUX 입력, 헤드폰 출력, 앱 효과음용 오디오 코덱은 후속 하드웨어 단계
- microSD: 미디어, 테마, 설정, 제한된 스크립트 앱과 Retro-Go급 게임 콘텐츠

### 기본 옵션

- 전용 TRS 저항 래더 6키 컨트롤러
- 컨트롤러가 없어도 풋스위치와 본체 기본 동작은 유지한다.

### 외부 USB 옵션: GG Analog Meter

- 움직이는 바늘을 가진 물리 아날로그 미터를 **GG의 보조 디스플레이**로 사용한다.
- 오디오 케이블은 미터에 연결하지 않는다. GG가 교정한 RMS·VU·peak·입력
  Vrms/dBu/dBV/dBFS 값을 USB로 전송한다.
- GG는 USB Host, 미터는 vendor-defined HID 장치로 시작한다. 20~30Hz 상태 패킷,
  장치 내부 DAC/PWM·저역통과·바늘 드라이버를 기본 구조로 한다.
- USB 전력 안에서 동작하도록 설계하되, 별도 전지를 쓰더라도 USB GND는 연결되므로
  전지가 자동으로 갈바닉 절연을 보장하지는 않는다.
- 미터는 분석 결과를 표시할 뿐 메인 기타 신호나 Thru 경로의 일부가 아니다.

## 3. 오디오 불변조건

### 무손실 Thru

`INST IN tip -> THRU OUT tip`은 릴레이·ADC·DSP·펌웨어를 통과하지 않는 직접 아날로그
경로다. 전원 꺼짐과 소프트웨어 오류에도 신호가 통과해야 한다. 분석 탭의 입력 임피던스,
커패시턴스와 접지 배치가 Thru에 주는 부하만 관리한다.

### 정확한 분석 탭

- GG 목표 회로는 사용자 LINE/INST 게인 스위치를 제거한다.
- 입력을 두 고임피던스 가지로 나눠 PCM1808의 두 채널에서
  **HOT 약 0.13x**와 **SENSITIVE 약 3.98x** 두 범위로 동시에 샘플링한다. HOT은
  op-amp 전에 주파수 보상 수동 분압을 거쳐 9V 전원 레일보다 큰 입력도 받는다. 명목
  sine 입력 한계는 약 8.1Vrms라 +4dBu 장비의 피크 여유도 확보한다.
- 목표 버퍼는 9V 단전원에서 동작하는 RRIO·저바이어스 OPA2192다. 현재 TL072는
  구형 브레드보드 기준 측정에만 사용하고, 부품 조달과 별도 목표 회로 작업 뒤 교체한다.
- DSP는 포화되지 않은 범위를 overlap+hysteresis로 선택하고, 각 경로의 교정값을 이용해
  입력잭 전압으로 역산한다. 앱 전환이나 사용자가 범위를 고르는 과정은 없다.
- 분석 탭의 목표 유효 입력 임피던스는 최소 5Mohm이고 현재 목표값은 약 7.56Mohm이다.
  수동 픽업 직결과 이펙터 출력 양쪽에서 Thru의 주파수 응답과 레벨을 눈에 띄게
  바꾸지 않아야 한다.
- 최종 교정은 범위별 1kHz gain뿐 아니라 20Hz~20kHz sweep 보정표를 포함한다.

현재 브레드보드의 `LINE 2.00x / INST 7.82x` SPDT 회로는 **구형 프로토타입**이다.
실물 연결은 `hardware/AS_BUILT_WIRING.md`, 다음 적용안은 `ASSEMBLY.md`에 두고,
목표 회로는 듀얼채널 하드웨어와 부품이 함께 준비된 뒤 별도 작업으로 전환한다.

### 레벨 표시의 의미

- 절대량의 기준값은 교정된 **입력잭 Vrms**다.
- `dBV = 20 log10(Vrms / 1V)`, `dBu = 20 log10(Vrms / 0.775V)`를 사용한다.
- 사용자용 dBFS는 현재 선택된 ADC 채널이 아니라, 제품에서 선언한 하나의
  **GG Input Full Scale**을 기준으로 한다. 범위가 바뀌어도 표시가 뛰지 않아야 한다.
- ADC 채널 자체의 dBFS와 현재 선택 범위는 진단 정보이며 기본 계기 화면의 기준이 아니다.
- Instrument, Consumer(-10dBV), Pro(+4dBu) 표시는 비교용 기준선이다. 실제 장비 출력의
  허용 한계나 품질 판정을 단정하는 라벨로 사용하지 않는다.
- `LIVE`, `AVG 1s`, `AVG 3s`, sample peak hold를 제공해 순간값과 체감 평균을 함께 비교한다.

이 계약의 목적은 여러 사용자가 서로 다른 페달보드 출력을 같은 GG에 번갈아 연결하고,
동일한 단위와 시간창으로 레벨을 맞출 수 있게 하는 것이다.

## 4. 기본 앱과 콘텐츠

### 현재 구현

- Sound Monitor: `Curve / 12-Band / Circular / Reference`
- Tuner
- dB Meter
- Gallery: SD `GG/images`의 JPG/PNG/BMP/GIF/BIN 탐색·표시
- Bounce

### GG 목표 기본 앱

- Monitor, Tuner, dB Meter, Metronome, Gallery, Music, Bounce
- Gallery와 Music은 SD의 이미지·음악을 사용한다.
- Music, Metronome과 앱 효과음은 향후 코덱의 헤드폰 출력으로만 재생한다. 하드와이어
  Thru에 소프트웨어 오디오를 섞지 않는다.

## 5. SD와 소프트웨어 확장 범위

- SD가 없어도 본체와 내장 앱은 부팅하고 동작한다.
- SD의 표준 루트는 `GG/images`, `GG/music`, `GG/roms`다. Gallery와 세 카탈로그
  기반은 구현됐고 Music 재생·Retro 실행은 아직 구현되지 않았다.
- SD는 이미지·음악·게임 샘플, 테마, 설정, 업데이트 패키지를 제공한다.
- 펌웨어 업데이트 없이 추가하는 앱은 서명·권한·자원 제한이 가능한 스크립트/바이트코드
  런타임을 장기 목표로 한다. 임의의 ESP32 네이티브 바이너리를 직접 실행하지 않는다.
- GG의 게임 범위는 Retro-Go급 8/16비트 에뮬레이션까지다. GBA와 NDS는 GG 범위가 아니다.
- GBA/NDS, 터치 중심 UI, 더 높은 연산량과 오디오 인터페이스 기능은 차기 하드웨어
  **GG2** 검토 범위다.

## 6. PC 기준 앱

- PC 시뮬레이터는 단순 화면 시험기가 아니라 GG의 앱·런처·설정을 먼저 완성하는 기준
  구현이며, GG 없이도 사용할 수 있는 독립 데스크톱 앱이다.
- PC 시스템 오디오/캡처 장치는 PCM1808을, 로컬 `GG/` 폴더는 MicroSD를, PC 오디오
  출력은 미래 헤드폰/AUX 코덱을 대신한다.
- 앱·UI·상태·설정 의미는 PC와 GG가 공유하고 하드웨어 차이만 플랫폼 백엔드로 분리한다.
- Music·Metronome·미디어·게임은 PC에서 실제 사용자 흐름을 먼저 완성한 뒤 같은 공통
  코드를 ESP에 이식한다. 상세 계약과 순서는 `PC_SIMULATOR_PRODUCT.md`가 권위다.

## 7. GG와 GG2의 경계

| 항목 | GG | GG2 검토 |
|---|---|---|
| 주 정체성 | 무손실 Thru 사운드 디스플레이 | 고성능 오디오·휴대형 컴퓨팅 |
| 입력 UI | 풋스위치 + 옵션 6키 | 기본 터치 UI |
| 게임 | Retro-Go급 | GBA/NDS 검토 |
| USB 오디오 인터페이스 | 범위 밖 | 검토 |
| 외부 화면 | GG Analog Meter 1종 | 듀얼 일반/터치 화면 검토 |
| 메인 오디오 | 하드와이어 Thru | Thru 보존을 전제로 별도 아키텍처 검토 |

ESP32-S3에는 Bluetooth Classic이 없으므로 GG의 Bluetooth 오디오(A2DP)는 범위에서
제외한다. BLE는 MIDI·제어·설정 연동에 사용할 수 있다.
