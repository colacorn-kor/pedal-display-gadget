# GG 현재 브레드보드 배선표

> 이 문서는 작업대에서 보는 **현재 프로토타입 배선표**다. 준비물, 조립 순서, 폐기된
> 회로, 부품 제거 절차와 미래 개조안은 싣지 않는다. 표에 있는 연결이 현재 브레드보드의
> 기준이다.

## 현재 상태

| 영역 | 상태 | 이 문서의 기준 |
|---|---|---|
| 디스플레이·PCM1808·TRS 6키·풋스위치 | 연결됨 | 아래 표와 대조 |
| MicroSD `SZH-EKBZ-005` | 배선 완료, 기능 확인 대기 | VCC=`+5V`, G11/G12/G13/G47 |
| 오디오 분석 탭 | 재배선 필요 | 현재 보유한 `TL072 + LINE/INST SPDT` 회로 |
| 뮤트 | 미연결 | G3도 J201도 연결하지 않음 |
| 자동 듀얼레인지 | 미래 회로 | OPA2192 미보유. 이 문서에 포함하지 않음 |

미래 자동 듀얼레인지 회로는 `hardware/NETLIST_SPEC.md`, 설계와 교정 메모는
`hardware/AUDIO_FRONTEND_ENGINEERING.md`가 맡는다. 지금 브레드보드를 연결할 때는
그 두 문서를 볼 필요가 없다.

## 반드시 지킬 것

- **USB와 외부 9V를 동시에 연결하지 않는다.** 사용자가 별도로 알리지 않는 동안은
  외부 9V가 분리된 상태로 본다.
- MP1584 출력은 다른 보드에 연결하기 전에 `5.0V`인지 확인한다.
- TRS 컨트롤러 Ring은 `+3V3` 전용이다. `+5V`를 연결하지 않는다.
- G3를 J201 게이트에 직결하지 않는다. 현재 뮤트 회로는 미연결이다.
- 모든 모듈과 두 오디오 잭의 GND는 공통 GND에 연결한다.

## 1. 전원 배선

| 네트 | 서로 연결할 점 |
|---|---|
| `+9V_RAW` | ELB040202 `+` ↔ 1N5819 애노드(띠 없는 쪽) |
| `+9V_PROT` | 1N5819 캐소드(띠 쪽) ↔ MP1584 `IN+` ↔ 100Ω 한쪽 |
| `+9V_OPAMP` | 위 100Ω 반대쪽 ↔ TL072 pin 8 ↔ 100uF `+` ↔ 100nF 한쪽 |
| `+5V` | MP1584 `OUT+` ↔ DevKit `5V` ↔ PCM1808 `+5V` ↔ SD 모듈 `VCC` |
| `+3V3` | DevKit `3V3` ↔ ST7796 `VCC` ↔ PCM1808 `3.3` ↔ TRS Ring 보호저항 |
| `GND` | ELB040202 `-`, MP1584 `IN-`/`OUT-`, DevKit GND, ST7796 GND, PCM1808 GND, SD GND, TL072 pin 4, 두 오디오 잭 Sleeve, TRS Sleeve, 풋스위치 GND |

`+9V_OPAMP`의 100uF `-`와 100nF 반대쪽은 GND에 연결한다. 가능하면 이 두 콘덴서는
TL072 가까이에 둔다. 외부 9V가 없고 USB만 연결된 동안 `+5V`와 `+3V3`는 켜지지만
`+9V_OPAMP`와 TL072는 꺼져 있으므로 오디오 분석 결과는 유효하지 않다.

```text
ELB +9V -> 1N5819(띠가 부하 쪽) -> +9V_PROT -> MP1584 -> +5V
                                           `-> 100R -> +9V_OPAMP
DevKit 3V3 -------------------------------------------------> +3V3
모든 GND ----------------------------------------------------> GND
```

## 2. ESP32 GPIO 배정

| GPIO | 연결 대상 |
|---:|---|
| G1 | ST7796 `BL` |
| G2 | ST7796 `CS` |
| G3 | **연결하지 않음**. 미래 뮤트 제어 예약 |
| G4 | 220Ω을 거쳐 메인 TRS 잭 `Tip` |
| G8 | PCM1808 `SCKI` |
| G9 | PCM1808 `BCK` |
| G10 | PCM1808 `OUT`/`DOUT` |
| G11 | SD `MISO` |
| G12 | ST7796 `SCL` + SD `SCK` 공유 |
| G13 | ST7796 `SDA` + SD `MOSI` 공유 |
| G14 | ST7796 `RST` |
| G17 | 풋스위치 한쪽 접점 |
| G18 | PCM1808 `LRC`/`LRCK` |
| G21 | ST7796 `DC` |
| G47 | SD `CS` |

G5, G6, G7, G15, G16, G40, G41, G42는 현재 연결하지 않는다.

## 3. ST7796 디스플레이

| ST7796 | 연결 |
|---|---|
| `GND` | GND |
| `VCC` | +3V3 |
| `SCL` | G12, SD SCK와 공유 |
| `SDA` | G13, SD MOSI와 공유 |
| `RST` | G14 |
| `DC` | G21 |
| `CS` | G2 |
| `BL` | G1 |
| `SDA-0` | 연결하지 않음 |

## 4. PCM1808 오디오 ADC

| PCM1808 | 연결 |
|---|---|
| `+5V` | +5V |
| `3.3` | +3V3 |
| `GND` | GND |
| `SCKI` | G8 |
| `BCK` | G9 |
| `OUT`/`DOUT` | G10 |
| `LRC`/`LRCK` | G18 |
| `VINL` | 아래 TL072 pin 1 출력에서 1uF 커플링 콘덴서를 거친 신호 |
| `VINR` | 현재 연결하지 않음 |

PCM1808 모드 점퍼는 **ESP32가 클럭을 공급하는 Slave + I2S 형식**으로 둔다. 모듈마다
MD0/MD1 점퍼 표기가 다르므로 해당 모듈의 실크와 데이터시트에서 `Slave`, `I2S`를 확인한다.

## 5. MicroSD `SZH-EKBZ-005`

| SD 모듈 | 연결 |
|---|---|
| `GND` | GND |
| `VCC` | **+5V** |
| `MISO` | G11 |
| `MOSI` | G13, 디스플레이 SDA/MOSI와 공유 |
| `SCK` | G12, 디스플레이 SCL/SCK와 공유 |
| `CS` | G47 |

이 모듈은 VCC 4.5~5.5V용 온보드 레귤레이터와 레벨 변환 회로가 있다. `VCC`를
`+3V3`에 연결하지 않는다. 카드는 전원을 끈 상태에서 넣거나 뺀다.

## 6. 컨트롤러와 풋스위치

### 메인 보드의 TRS 잭

| TRS 접점 | 연결 |
|---|---|
| `Sleeve` | GND |
| `Ring` | 100Ω을 거쳐 +3V3 |
| `Tip` | 220Ω을 거쳐 G4 |

### 6키 컨트롤러 내부

| 연결 | 값 |
|---|---:|
| Ring ↔ Rtop ↔ Tip | 10kΩ |
| Tip ↔ UP 버튼 ↔ Sleeve | 0Ω |
| Tip ↔ DOWN 버튼 ↔ Sleeve | 470Ω |
| Tip ↔ LEFT 버튼 ↔ Sleeve | 1kΩ |
| Tip ↔ RIGHT 버튼 ↔ Sleeve | 2kΩ |
| Tip ↔ OK 버튼 ↔ Sleeve | 4.7kΩ |
| Tip ↔ HOME 버튼 ↔ Sleeve | 10kΩ |

각 키의 저항과 버튼은 Tip-Sleeve 사이에 직렬로 연결한다. TRS 케이블의 Ring은 +3V3,
Sleeve는 GND, Tip은 판정 신호다.

### 풋스위치

| 접점 | 연결 |
|---|---|
| 평소 열려 있는 접점 한쪽 | G17 |
| 평소 열려 있는 접점 반대쪽 | GND |

## 7. 현재 오디오 입력과 하드와이어 Thru

이 절은 지금 재배선할 회로다. **현재 보유한 TL072와 SPDT를 사용하며 OPA2192는 필요 없다.**

### 오디오 잭과 분석 탭

| 시작점 | 연결 | 도착점 |
|---|---|---|
| 입력잭 `Tip` | 직결 | 출력잭 `Tip` |
| 입력잭 `Sleeve` | 공통 GND | 출력잭 `Sleeve` |
| 입력잭 `Tip` | 1uF 커플링 콘덴서 | `ANALYZER_IN` |
| `ANALYZER_IN` | 직결 | TL072 pin 3 (`+INA`) |
| `ANALYZER_IN` | 1MΩ | `VREF` |

입력잭 Tip과 출력잭 Tip 사이에는 스위치, IC, 저항이나 콘덴서를 직렬로 넣지 않는다.
Thru 신호는 전원과 펌웨어에 상관없이 이 직결선을 통과한다.

### VREF 4.5V

| 시작점 | 부품/연결 | 도착점 |
|---|---|---|
| `+9V_OPAMP` | 10kΩ | `VREF_DIV` |
| `VREF_DIV` | 10kΩ | GND |
| `VREF_DIV` | 100uF (`+`가 VREF_DIV) | GND |
| `VREF_DIV` | 100nF | GND |
| `VREF_DIV` | 직결 | TL072 pin 5 (`+INB`) |
| TL072 pin 7 (`OUTB`) | 직결 | TL072 pin 6 (`-INB`) |
| TL072 pin 7 | 이 점을 `VREF`로 사용 | 아래 게인 회로 |

### TL072 신호와 LINE/INST 스위치

| 시작점 | 부품/연결 | 도착점 |
|---|---|---|
| TL072 pin 8 | 직결 | `+9V_OPAMP` |
| TL072 pin 4 | 직결 | GND |
| TL072 pin 2 (`-INA`) | 15kΩ 피드백 저항 | TL072 pin 1 (`OUTA`) |
| TL072 pin 2 | 15kΩ | SPDT `LINE` 쪽 단자 |
| TL072 pin 2 | 2.2kΩ | SPDT `INST` 쪽 단자 |
| SPDT 공통 단자 | 직결 | `VREF` |
| TL072 pin 1 | 1uF 커플링 콘덴서 | PCM1808 `VINL` |

`LINE`의 전압 이득은 약 2.00배, `INST`는 약 7.82배다. 두 게인 저항의 기준점은 GND가
아니라 반드시 `VREF`다. 1uF에 극성이 있으면 두 커플링 위치 모두 `+` 쪽을 TL072 쪽에
둔다. 필름 또는 무극성 콘덴서는 방향이 없다.

```text
입력 Tip ------------------------------ 출력 Tip
    `-- 1uF -- ANALYZER_IN -- TL072 -- 1uF -- PCM1808 VINL
```

## 8. 현재 연결하지 않는 것

| 대상 | 상태 |
|---|---|
| G3와 J201 | 둘 사이를 연결하지 않음. 뮤트 드라이버 확정 대기 |
| PCM1808 `VINR` | 현재 비움 |
| ST7796 `SDA-0` | 비움 |
| G5/G6/G7/G15/G16 | 비움 |
| G40/G41/G42 | 미래 오디오 출력/제어용으로 비움 |
| MIDI, AUX, 헤드폰 코덱 | 현재 배선 없음 |

## 9. 배선 대조값

| 확인점 | 정상 값/상태 |
|---|---|
| 전원 OFF, 입력 Tip ↔ 출력 Tip | 거의 0Ω |
| 전원 OFF, 각 전원 레일 ↔ GND | 단락 아님 |
| 외부 9V만 연결, `+9V_PROT` | 약 8.6V |
| 외부 9V만 연결, `+5V` | 4.9~5.1V |
| 외부 9V만 연결, `+3V3` | 3.2~3.4V |
| 외부 9V만 연결, `VREF` | `+9V_OPAMP`의 약 절반 |
| SD VCC ↔ +3V3 | 서로 직접 연결되지 않음 |
| PCM1808 VINL ↔ VINR | 서로 연결되지 않음 |
